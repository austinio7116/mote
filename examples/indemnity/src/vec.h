/*
 * Indemnity Run — vector/matrix helpers.
 *
 * Now SHARED with the engine: this forwards to the engine's mote_vec.h (the
 * engine's was distilled from this file, so the two are identical). Sharing it
 * means Indemnity's Vec3/Mat3 ARE the engine's types, so every source file can
 * call the Mote engine ABI directly with no conversion.
 */
#ifndef ELITE_VEC_H
#define ELITE_VEC_H

#include "mote_vec.h"

#endif
